
while ((len = get_line(str)) > 0)
	// разбираем строку:
	while(есть символы в строке)
		читаем слово
		if (слово == число)
			послать в стек
		else if (оператор или функция)
			if (оператор)
				взять из стека операцию
				выполнить операцию
				результат послать в стек
			else if (числовая функция)
				взять из стека число
				применить функцию
				результат послать в стек
			else if (имя переменной)
				if (переменная свбодна)
					взять из стека число
					записать в переменную
				else
					взять из переменной число
					послать число в стек
			else if (коменда над стеком)
				выполнить команду над стеком
			else
				ошибка

	// новая строка
	взять с вершины стека число, напечать, внесть в переменную last_print