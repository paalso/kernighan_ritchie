// !!!! TEST INPUT FOR 1-23 !!!

// Упражнение 1.7. Напишите про// грамму, печатающую значение EOF.
/* int max_array_element_index(int* array, int len) {
int max_index = 0;
*/
#include <stdio.h>
/* int max_array_element_index(int* array, int len) {
    int max_index = 0;
    int max = array[max_index];
    for (int i = 1; i < len; ++i) { */
int main(void)
{
    char s[] = " AAZ/* dsc;jSCN */";
    printf("%d %c /* avc */\n",EOF,EOF ); // once more
    printf("%d %c // \n",EOF,EOF ); // once more
    // +6565
    // -1 
// Упражнение 1.7. Напишите про// грамму, печатающую значение EOF.
// Упражнение 1.7. "Напишите про// грамму, печатающую значение EOF."
return /* int max_array_element_index(int* array, int len) {
int max_index = 0;
int max = array[max_index];
for (int i = 1; i < len; ++i) { */

/* deefef */ 0;
}
// Упражнение 1.7. Напишите про// грамму, печатающую значение EOF.